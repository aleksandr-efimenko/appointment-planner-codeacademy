import './App.css';
import { BrowserRouter, Route, RedirectFunction, NavLink } from 'react-router-dom';

function App() {
  const ROUTES = {
    CONTACTS: "/contacts",
    APPIONTMENTS: "/appointments",
  };

  return (
    <>
      <nav>
      </nav>
    </>
  );
}

export default App;
